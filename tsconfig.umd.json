{
    "extends": "./tsconfig.base",
    "compilerOptions": {
        "module": "umd", /* Specify module code generation: 'none', 'commonjs', 'amd', 'system', 'umd', 'es2015', or 'ESNext'. */
        "outDir": "./dist/umd", /* Redirect output structure to the directory. */
        "declaration": true
    }
}